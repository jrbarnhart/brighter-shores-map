import { Card, CardHeader, CardTitle } from "../ui/card";

export default function RoomCard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle></CardTitle>
      </CardHeader>
    </Card>
  );
}
